version: 0.1
backend:
  phases:
    build:
      commands:
        - cd simplq/
        - amplifyPush --simple
frontend:
  phases:
    build:
      commands: 
       - npm install
       - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths: []
